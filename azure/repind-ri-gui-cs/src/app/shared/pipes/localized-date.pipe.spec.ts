/* tslint:disable:no-unused-variable */

import { LocalizedDatePipe } from './localized-date.pipe';

describe('Pipe: LocalizedDatee', () => {
  it('create an instance', () => {
    const pipe = new LocalizedDatePipe(null);
    expect(pipe).toBeTruthy();
  });
});
