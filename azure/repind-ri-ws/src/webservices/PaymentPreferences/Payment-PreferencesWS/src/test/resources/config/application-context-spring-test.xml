<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-2.5.xsd 
	http://www.springframework.org/schema/tx 
	http://www.springframework.org/schema/tx/spring-tx-2.5.xsd 
	http://www.springframework.org/schema/context 
	http://www.springframework.org/schema/context/spring-context-2.5.xsd">

	<!-- Enterprise layer's dependencies -->
	<import resource="classpath:ioc/0-repind_pp-model-V2-enterpriselayer-dependencies.xml" />
	<!--  Web Service definitions -->
	<import resource="application-wscontext-service.xml" />
 	
	<!-- Cool object to find a bean from Anywhere -->
	<bean id="springLocator"
		class="com.afklm.jraf.bootstrap.locator.SpringLocator">
	</bean>

	<!-- To precise the persistence configuration name file -->
	<bean id="persistenceUnitManager"
		class="org.springframework.orm.jpa.persistenceunit.DefaultPersistenceUnitManager">
		<property name="persistenceXmlLocations">
			<list>
				<value>META-INF/persistence-web-test.xml</value>
			</list>
		</property>
	</bean>

	<!-- EntityManagerFactory definition : JPA one -->
	<bean id="paymentEmf"
		class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="persistenceUnitManager"
			ref="persistenceUnitManager" />
		<property name="persistenceUnitName" value="myPersistenceUnit" />
	</bean>
	
	<!-- TransactionManager JPA one -->
	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="paymentEmf" />
	</bean>
	
	<!-- Jraf adhesion provider -->
	<!-- bean id="adhesion"
 		class="com.afklm.jraf.provider.adhesion.AdhesionProviderSpringImpl" init-method="initialize">
	 	<property name="configFile">
		 <value>WEB-INF/config/adhesion.cfg.xml</value>
	 	</property>	
	</bean-->
	
	
	<!-- Enable annotation usage for transaction -->
	<tx:annotation-driven transaction-manager="transactionManager" />
	
	<!-- Enable the annotation usage (bean injection for instance) -->
		<context:annotation-config />
		<context:component-scan base-package="com.airfrance.paymentpreferenceswsv2" />
		<context:component-scan base-package="com.airfrance.paymentpreferenceswsv2.v2" />

</beans>
