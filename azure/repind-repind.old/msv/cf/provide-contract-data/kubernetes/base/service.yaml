apiVersion: v1
kind: Service
metadata:
  name: provide-contract-data
  labels:
    app.kubernetes.io/name: provide-contract-data
    app.kubernetes.io/instance: provide-contract-data
spec:
  selector:
    app.kubernetes.io/name: provide-contract-data
    app.kubernetes.io/instance: provide-contract-data
  ports:
    - port: 8080               # Port for the service
      targetPort: 8080         # Port of the container (should be the same as in your deployment)