apiVersion: v1
kind: Service
metadata:
  name: provide-preference-data
  labels:
    app.kubernetes.io/name: provide-preference-data
    app.kubernetes.io/instance: provide-preference-data
spec:
  selector:
    app.kubernetes.io/name: provide-preference-data
    app.kubernetes.io/instance: provide-preference-data
  ports:
    - port: 8080               # Port for the service
      targetPort: 8080         # Port of the container (should be the same as in your deployment)