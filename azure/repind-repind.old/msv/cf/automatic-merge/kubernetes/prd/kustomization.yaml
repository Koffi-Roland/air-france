bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -prd

namespace: repind-prd

commonLabels:
  app.kubernetes.io/name: automatic-merge
  app.kubernetes.io/instance: automatic-merge-prd

images:
  - name: harbor-vlb.af-klm.com/cdm/automatic-merge

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-rollout.yaml
  - deploy-profile.yaml