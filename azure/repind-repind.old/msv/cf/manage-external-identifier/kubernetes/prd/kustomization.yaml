bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -prd

namespace: repind-prd

commonLabels:
  app.kubernetes.io/name: manage-external-identifier
  app.kubernetes.io/instance: manage-external-identifier-prd

images:
  - name: harbor-vlb.af-klm.com/cdm/manage-external-identifier

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-extra-options.yaml
  - deploy-profile.yaml