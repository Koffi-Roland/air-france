apiVersion: v1
kind: Service
metadata:
  name: provide-individual-score
  labels:
    app.kubernetes.io/name: provide-individual-score
    app.kubernetes.io/instance: provide-individual-score
spec:
  selector:
    app.kubernetes.io/name: provide-individual-score
    app.kubernetes.io/instance: provide-individual-score
  ports:
    - port: 8080               # Port for the service
      targetPort: 8080         # Port of the container (should be the same as in your deployment)
