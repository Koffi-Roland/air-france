apiVersion: v1
kind: Service
metadata:
  name: provide-contact-data
  labels:
    app.kubernetes.io/name: provide-contact-data
    app.kubernetes.io/instance: provide-contact-data
spec:
  selector:
    app.kubernetes.io/name: provide-contact-data
    app.kubernetes.io/instance: provide-contact-data
  ports:
    - port: 8080               # Port for the service
      targetPort: 8080         # Port of the container (should be the same as in your deployment)