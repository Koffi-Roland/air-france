apiVersion: v1
kind: Service
metadata:
  name: manage-tribes
  labels:
    app.kubernetes.io/name: manage-tribes
    app.kubernetes.io/instance: manage-tribes
spec:
  selector:
    app.kubernetes.io/name: manage-tribes
    app.kubernetes.io/instance: manage-tribes
  ports:
    - port: 8080               # Port for the service
      targetPort: 8080         # Port of the container (should be the same as in your deployment)