bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -ite

namespace: repind-ite

commonLabels:
  app.kubernetes.io/name: manage-doctor
  app.kubernetes.io/instance: manage-doctor-ite

images:
  - name: harbor-vlb.af-klm.com/cdm/manage-doctor

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-extra-options.yaml
  - deploy-profile.yaml