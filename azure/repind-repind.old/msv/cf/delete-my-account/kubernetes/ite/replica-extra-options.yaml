apiVersion: apps/v1
kind: Deployment
metadata:
  name: delete-my-account
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    spec:
      containers:
        - name: delete-my-account
          readinessProbe:
            httpGet:
              path: /infra/healthcheck
