bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -acp

namespace: repind-acp

commonLabels:
  app.kubernetes.io/name: provide-identification-data
  app.kubernetes.io/instance: provide-identification-data-acp

images:
  - name: harbor-vlb.af-klm.com/cdm/provide-identification-data

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-extra-options.yaml
  - deploy-profile.yaml