bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -acp

namespace: repind-acp

commonLabels:
  app.kubernetes.io/name: search-gin-by-contract
  app.kubernetes.io/instance: search-gin-by-contract-acp

images:
  - name: harbor-vlb.af-klm.com/cdm/search-gin-by-contract

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-extra-options.yaml
  - deploy-profile.yaml