apiVersion: v1
kind: Service
metadata:
  name: search-gin-by-contract
  labels:
    app.kubernetes.io/name: search-gin-by-contract
    app.kubernetes.io/instance: search-gin-by-email
spec:
  selector:
    app.kubernetes.io/name: search-gin-by-contract
    app.kubernetes.io/instance: search-gin-by-contract
  ports:
    - port: 8080               # Port for the service
      targetPort: 8080         # Port of the container (should be the same as in your deployment)