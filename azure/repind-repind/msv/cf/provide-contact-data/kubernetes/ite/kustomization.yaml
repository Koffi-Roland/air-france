bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -ite

namespace: repind-ite

commonLabels:
  app.kubernetes.io/name: provide-contact-data
  app.kubernetes.io/instance: provide-contact-data-ite

images:
  - name: harbor-vlb.af-klm.com/cdm/provide-contact-data

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-extra-options.yaml
  - deploy-profile.yaml