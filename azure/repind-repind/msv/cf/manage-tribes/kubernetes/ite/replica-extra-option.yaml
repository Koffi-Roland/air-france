apiVersion: apps/v1
kind: Deployment
metadata:
  name: manage-tribes
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    spec:
      containers:
        - name: manage-tribes
          readinessProbe:
            httpGet:
              path: /infra/healthcheck
