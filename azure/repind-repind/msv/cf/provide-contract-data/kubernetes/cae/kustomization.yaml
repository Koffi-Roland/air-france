bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -cae

namespace: repind-cae

commonLabels:
  app.kubernetes.io/name: provide-contract-data
  app.kubernetes.io/instance: provide-contract-data-cae

images:
  - name: harbor-vlb.af-klm.com/cdm/provide-contract-data

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-extra-options.yaml
  - deploy-profile.yaml