bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -prd

namespace: repind-prd

commonLabels:
  app.kubernetes.io/name: delete-my-account
  app.kubernetes.io/instance: delete-my-account-prd

images:
  - name: harbor-vlb.af-klm.com/cdm/delete-my-account

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-extra-options.yaml
  - deploy-profile.yaml