apiVersion: v1
kind: Service
metadata:
  name: manage-doctor
  labels:
    app.kubernetes.io/name: manage-doctor
    app.kubernetes.io/instance: manage-doctor
spec:
  selector:
    app.kubernetes.io/name: manage-doctor
    app.kubernetes.io/instance: manage-doctor
  ports:
    - port: 8080               # Port for the service
      targetPort: 8080         # Port of the container (should be the same as in your deployment)