bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -cae

namespace: repind-cae

commonLabels:
  app.kubernetes.io/name: forget-me-asked
  app.kubernetes.io/instance: forget-me-asked-cae

images:
  - name: harbor-vlb.af-klm.com/cdm/forget-me-asked

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-rollout.yaml
  - deploy-profile.yaml