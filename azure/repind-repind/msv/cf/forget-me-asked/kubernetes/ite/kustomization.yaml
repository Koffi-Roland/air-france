bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -ite

namespace: repind-ite

commonLabels:
  app.kubernetes.io/name: forget-me-asked
  app.kubernetes.io/instance: forget-me-asked-ite

images:
  - name: harbor-vlb.af-klm.com/cdm/forget-me-asked

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-extra-options.yaml
  - deploy-profile.yaml