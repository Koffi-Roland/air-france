bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -cae

namespace: repind-cae

commonLabels:
  app.kubernetes.io/name: manage-external-identifier
  app.kubernetes.io/instance: manage-external-identifier-cae

images:
  - name: harbor-vlb.af-klm.com/cdm/manage-external-identifier

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-extra-options.yaml
  - deploy-profile.yaml