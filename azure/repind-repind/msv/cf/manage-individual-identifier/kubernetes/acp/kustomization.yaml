bases:
  - ../base

resources:
  - ingress.yaml

nameSuffix: -acp

namespace: repind-acp

commonLabels:
  app.kubernetes.io/name: manage-individual-identifier
  app.kubernetes.io/instance: manage-individual-identifier-acp

images:
  - name: harbor-vlb.af-klm.com/cdm/manage-individual-identifier

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - replica-rollout.yaml
  - deploy-profile.yaml