apiVersion: v1
kind: Service
metadata:
  name: manage-individual-identifier
  labels:
    app.kubernetes.io/name: manage-individual-identifier
    app.kubernetes.io/instance: manage-individual-identifier
spec:
  selector:
    app.kubernetes.io/name: manage-individual-identifier
    app.kubernetes.io/instance: manage-individual-identifier
  ports:
    - port: 8080               # Port for the service
      targetPort: 8080         # Port of the container (should be the same as in your deployment)